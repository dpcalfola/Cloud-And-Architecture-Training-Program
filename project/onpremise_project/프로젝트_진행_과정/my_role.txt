금 오후부터 월화까지
    - 도커 서버 구축
        1. DB
        2. FTP
        3. DHCP
        4. WEB 3tier
        5. DNS

     - 도커 데스크탑 설정 관련 문서화
        - 도커 볼륨 생성
        - 도커 네트워크 생성
            - 개별 네트워크 주소대역 명시
        - 도커 컨테이너 생성 명령어








< 수시 일과 - PPT 보완 >


PPT 수정:

	1. 대분류 재정의
	    사업개요 ->  추진목적 -> 회사소개 -> 솔루션  기대효과


	2. 추가 항목
		- 제안 배경
		- 회사소개 (조직도 포함)


	3. 기술적인 부분 포함
	    - 목적 -> 기술세부 -> 기대효과
	        - 대신 간단하게 (자세한 설명은 기술문서로)
	    - 대제목 명확히


    5. 발표상 전달 오류
       - 전체구성도에서 백본간의 연결이 active - avtive 임을 명시
       - 인터넷 망 연결 경로를 우선순위로(신규-기존 순서) 언급할것



PPT 폰트 임포트 문제 해결할 것
안된다면 기본 폰트로 재장성

 -> 근데 왜 안되는지 모르겠고 윈도우에서 다시 시도 ?







서버 구축 변경사항

    (기존 구상) 윈도우 HOST 에서 구동하는 도커 데스크탑

    (변경안) VM ubuntu 의 Docker engine 에서 구동

        - VM 에 컨테이너 구성 방법
            - github repo 에서 코드를 가져와서 컨테이너 생성
            - repo 에 컨테이너 생성 관련 명령어 포함
            - Dependency: Docker engine, git

        - GNS3 연결 방법
            - 1안) 각 1개의 서버당 1개의 VM 으로 구성
            - 2안) 1개의 VM에 모든 컨테이너를 생성하고 V-box 에서 포트 포워딩
            - 3안) 1안과 2안을 통합하혀 구현 (3개의 VM)
                DATA VM(FTP, DB),
                DHCP VM,
                WEB VM(WEB-server, WAS, Web-DB DNS)




